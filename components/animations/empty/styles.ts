import { StyleSheet } from "react-native";
import { sizes } from "@/constants/ui";

const styles = StyleSheet.create({
  container: {
    width: sizes.elements.animation,
    height: sizes.elements.animation,
  },
});

export default styles;
